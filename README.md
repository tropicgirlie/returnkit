
  # ReturnKit

  This is a code bundle for ReturnKit. The original project is available at https://www.figma.com/design/yq2QeWikrqSwp8ian4EKnA/ReturnKit.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  